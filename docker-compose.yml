version: '3.6'

services:
  server:
    build:
      context: .
      dockerfile: ./Dockerfile
    networks:
      - net
    volumes:
      - './logs/server.log:/var/log/ssh_management/server.log'
  redis:
    image: 'redis:6-alpine'
    networks:
      - net
networks:
  net:
    driver: bridge
